---
title:
draft: true
---

Write the main page body here in Markdown.

Use this template for document-style pages. Add structured sidebar fields only when the page genuinely needs them.

Draft workflow:

1. Create the file as a draft.
2. Add optional frontmatter only when the page needs it.
3. Change `draft` to `false` when the page should render.
4. If the route still `404`s locally, restart `npm run dev`.
