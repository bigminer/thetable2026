---
title:
description:
draft: true
---

Write the series introduction here in Markdown.

Use this template for series entries. Keep the frontmatter flat and limited to series-level information such as title, description, artwork, optional dates, and draft status.

Create individual sermon, message, or podcast items in `messages/` instead of adding nested episode lists here.

Draft workflow:

1. Create the file as a draft.
2. Add optional frontmatter only when the series needs it.
3. Change `draft` to `false`.
4. If the new route still `404`s locally, restart `npm run dev`.
