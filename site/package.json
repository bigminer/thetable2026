{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "dev:tailscale": "bash scripts/dev-tailscale.sh",
    "build": "astro build",
    "build:gh-pages": "ASTRO_GH_PAGES=true astro build --config astro.config.gh-pages.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "ask": "node --env-file-if-exists=.env --import tsx scripts/ask.ts",
    "fetch": "yt-dlp --write-auto-subs --sub-lang en --skip-download --write-info-json --download-archive scripts/transcripts/archive.txt --break-on-existing --match-filters \"title !*= 📱\" --output 'scripts/transcripts/raw/%(upload_date>%Y-%m-%d)s-%(id)s.%(ext)s' --convert-subs vtt --ignore-no-formats-error",
    "transcribe": "node --env-file-if-exists=.env --import tsx scripts/transcribe-missing.ts",
    "make-stubs": "node --import tsx scripts/make-stubs.ts",
    "enrich-titles": "node --import tsx scripts/enrich-titles.ts",
    "detect-bounds": "node --import tsx scripts/detect-bounds.ts",
    "make-descriptions": "node --env-file-if-exists=.env --import tsx scripts/make-descriptions.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.90.0",
    "@astrojs/node": "^10.0.6",
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.1.8",
    "dotenv": "^17.4.2",
    "openai": "^6.34.0",
    "sanitize-html": "^2.17.3"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
